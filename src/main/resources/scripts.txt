CREATE DATABASE users;

create table users
(
    username    varchar,
    last_name   varchar,
    id          bigint not null
        constraint users_pkey
            primary key,
    first_name  varchar,
    email       varchar,
    description varchar
);